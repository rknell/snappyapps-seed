describe('authentication', function() {

  beforeEach(module('admin'));

  it('should ...', inject(function(authentication) {

	//expect(authentication.doSomething()).toEqual('something');

  }));

});